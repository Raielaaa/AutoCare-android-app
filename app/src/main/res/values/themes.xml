<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AutoCare" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/Theme.color</item>
        <item name="colorPrimary">@color/Theme.color</item>
<!--        <item name="colorOnPrimary">@color/Theme.color.secondary</item>-->
    </style>

    <style name="Theme.AutoCare" parent="Base.Theme.AutoCare" />

    <style name="animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>